🏡 BelHomeFinder
Умный бот для поиска жилья в Беларуси

BelHomeFinder — это Telegram-бот, который автоматически ищет актуальные объявления о недвижимости в Беларуси, фильтрует по вашим критериям и отправляет только действительно подходящие варианты.
Он работает как персональный ассистент: ищет, сравнивает, сортирует и уведомляет — всё полностью автоматически.


✨ Основные возможности

🔍 Поиск жилья в реальном времени
Автоматический сбор объявлений из нескольких источников (в т.ч. Kufar).

🎯 Умные фильтры
Выберите цену, район, тип жилья — получайте только релевантные варианты.

🤖 Полностью автоматизированная работа
Бот сам отслеживает новые предложения и присылает уведомления.

📩 Удобные Telegram-уведомления
Новые варианты приходят прямо в чат — без лишнего шума.

🗄️ Хранение данных в базе
Повторяющиеся или старые объявления не будут присылаться.

⚙️ Конфигурируемая архитектура
Все параметры (токены, фильтры, параметры API) — в .env.

🚀 Быстрый старт
1. Клонируйте репозиторий:
```
git clone https://github.com/Mazavinka/BelHomeFinder

cd BelHomeFinder
```
2. Установите зависимости:

`pip install -r requirements.txt
`
3. Создайте .env файл

Используйте пример:

`cp .env.example .env
`

Заполните поля внутри файла .env:
```
TELEGRAM_BOT_TOKEN=...ваш токен...

DATABASE_URL=...sqlite/postgres...

KUFAR_API_KEY=...если требуется...
```
4. Запустите бота:

`python main.py
`

```BelHomeFinder/│
├── main.py          # Точка входа в приложение
├── tg.py            # Telegram-логика
├── parser.py        # Общий модуль парсинга объявлений
├── kufar_api.py     # Работа с Kufar API
├── db.py            # База данных и модели
├── messages.py      # Текстовые сообщения бота
├── signals.py       # Обработчики событий
│
├── requirements.txt # Зависимости проекта
└── .env.example     # Пример настроек окружения
```
Принцип работы:

1. Бот получает параметры поиска от пользователя.

2. Парсер собирает свежие объявления.

3. Проверяет, есть ли новинки.

4. Новые результаты отправляются пользователю в Telegram.

5. В базе данных сохраняется история, чтобы избежать дубликатов.


⚙️ Технологии:

• Python 3.10+

• aiogram / python-telegram-bot

• Requests / AIOHTTP

• SQLite / PostgreSQL

• Kufar API

• dotenv


🗺️ Планы развития:

• 🔄 Добавить рассылку по расписанию

• 📊 Аналитику цен и статистику районов

• 🧭 Поддержку дополнительных источников объявлений

• 🧠 ML-фильтр “подбор по предпочтениям”

• 💬 Мультиязычность (RU/BY/EN)






